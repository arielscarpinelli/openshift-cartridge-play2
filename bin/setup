#!/bin/bash

cd ${OPENSHIFT_DATA_DIR}
curl ${ACTIVATOR_SOURCE_URL} > activator.zip
unzip activator.zip
ln -s ${OPENSHIFT_DATA_DIR}/activator-${ACTIVATOR_INSTALLATION_VERSION} ${OPENSHIFT_DATA_DIR}/activator
rm activator.zip